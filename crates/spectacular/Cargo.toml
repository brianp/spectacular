[package]
name = "spectacular"
version = "0.1.0"
edition = "2024"
description = "An RSpec-inspired test framework for Rust with stackable before/after hooks"
license = "MIT"

[dependencies]
spectacular-macros = { path = "../spectacular-macros" }
